# 格式与交叉引用校对记录

## 校对范围

本次校对面向当前已整理的主要章节入口与跨章节链接：

- 第 3 章：器件与 MOS 晶体管模型
- 第 4 章：导线与互连模型
- 第 5 章：CMOS 反相器
- 第 6 章：CMOS 组合逻辑
- 第 7 章：时序逻辑
- 第 9 章：互连影响与应对
- 第 10 章：时序问题与时钟

## 本次发现并修复的问题

### 1. 第 9 章指向动态 CMOS 逻辑的链接目标不存在

第 9 章中已有两个位置引用：

```markdown
[[../06-CMOS组合逻辑/动态CMOS逻辑|动态CMOS逻辑]]
```

这些引用语义正确，因为电容串扰确实会影响 dynamic logic 的高阻动态节点。但第 6 章当前尚未建立 `动态CMOS逻辑.md` 文件，导致 Obsidian 链接落空。

本次修复：

- 新增 `06-CMOS组合逻辑/动态CMOS逻辑.md` 作为轻量入口页。
- 在 `06-CMOS组合逻辑/README.md` 中加入该入口。

### 2. 第 6 章 README 未展示 dynamic CMOS 后续入口

第 6 章 README 的目标中提到 dynamic logic，但知识点索引尚未提供可点击入口。

本次修复：

- 在“本章目标”中把 dynamic CMOS logic 改为指向 `[[动态CMOS逻辑]]`。
- 在“当前整理进度”中标记 6.3 已建立入口。
- 在“知识点索引”中新增 `[[动态CMOS逻辑]]`。

## 抽查结论

- 根目录 README 与 `00-总览/README.md` 当前已经指向第 10 章 `10-时序问题与时钟`，旧的 `10-测试与可靠性` 入口在 main 中已被移除。
- 第 4 章 `分布RC线.md` 已存在，能承接第 5 章中对 `分布RC线` 的交叉引用。
- 第 9 章到第 4 章互连模型、第 5 章缓冲链、第 6 章动态逻辑的主要跨章路径已经有目标文件。

## 本次没有做的事

- 没有重写正文内容。
- 没有补充新章节推导。
- 没有添加图片或裁剪 PDF。
- 没有把第 6 章 dynamic CMOS 的完整内容提前展开；该页只是交叉引用入口。

## 后续建议

- 第 6 章继续整理 6.2.2 ratioed logic、6.2.3 pass-transistor logic 和 6.3 dynamic CMOS 后，再把 `动态CMOS逻辑.md` 扩展为完整笔记。
- 后续每次新增跨章节链接时，优先检查目标文件是否已经存在；若内容尚未整理，可先建立轻量入口页。
